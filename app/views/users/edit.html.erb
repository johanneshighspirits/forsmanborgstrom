<% provide(:title, "Edit user") %>
<% provide(:button_text, "Save changes") %>
<% provide(:action_url, user_path(@user)) %>

<h1>Update profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>